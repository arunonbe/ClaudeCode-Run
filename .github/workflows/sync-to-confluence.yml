name: Sync All Layers to Confluence

on:
  push:
    branches: [main]
    paths:
      - 'Run 1/L1 Organism/**.md'
      - 'Run 1/L2 Molecule/**.md'
      - 'Run 1/L3 Codebase/**.md'
  workflow_dispatch:
    inputs:
      sync_all:
        description: 'Sync all layers (L1 + L2 + L3). Leave as true for full sync.'
        required: false
        default: 'true'

jobs:
  confluence-sync:
    name: Sync changed layers to Confluence
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests markdown

      - name: Detect changed files across all layers
        id: changes
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "files=" >> $GITHUB_OUTPUT
            echo "Manual trigger — full sync of L1, L2, and L3"
          elif git rev-parse HEAD~1 > /dev/null 2>&1; then
            FILES=$(git diff --name-only HEAD~1 HEAD -- 'Run 1/' | tr '\n' ' ')
            echo "files=$FILES" >> $GITHUB_OUTPUT
            echo "Changed files: $FILES"
          else
            echo "files=" >> $GITHUB_OUTPUT
            echo "First commit — syncing all layers"
          fi

      - name: Sync to Confluence
        env:
          ATLASSIAN_DOMAIN:     ${{ secrets.ATLASSIAN_DOMAIN }}
          ATLASSIAN_EMAIL:      ${{ secrets.ATLASSIAN_EMAIL }}
          ATLASSIAN_API_TOKEN:  ${{ secrets.ATLASSIAN_API_TOKEN }}
          CONFLUENCE_FOLDER_ID: ${{ secrets.CONFLUENCE_FOLDER_ID }}
        run: |
          python .github/scripts/sync_confluence.py ${{ steps.changes.outputs.files }}
